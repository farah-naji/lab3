let
  Source = curated_reviews,
  #"Kept top rows" = Table.FirstN(Source, 10000),
  #"Grouped rows" = Table.Group(#"Kept top rows", {"book_id"}, {{"avg_rating_book", each List.Average([rating]), type nullable number}, {"review_count_book", each Table.RowCount(_), Int64.Type}})
in
  #"Grouped rows"